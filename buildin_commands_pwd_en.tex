\TheCommand{pwd}%
\Command{pwd}

\TypeTable{pwd \\}{%
% properties
%\midrule
% operators
}

Prints the absolute path of the current working directory to the 
standard output. The current working
directory is stored in the \Variable*{PWD} variable.

\begin{lstlisting}[style=Groovybash, label={lst:example_pwd}, title={
Change and print the new current working directory.}]
cd "/some/place"
pwd
\end{lstlisting}


