%%
\label{par:readarray}
\parparameter{\parcommand{readarray} [atmost \partype{count}] [origin \partype{origin}] [discard \partype{count}] [from \partype{file}] [use \partype{callback}]}

Read lines from the standard input into the indexed array variable array, or from the file \partype{file} if specified.

