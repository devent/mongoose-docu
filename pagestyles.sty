\ProvidesPackage{pagestyles}[2012/02/10 Set the page styles for the document.]
\RequirePackage{suffix}

\title{\textsf{Fractal Dimension Analysis}}
\author{Erwin Mueller}
\date{\today{}}

%% set the memoir chapter style
\chapterstyle{madsen}

%%% set up the page layout
\setlrmarginsandblock{2.5cm}{*}{1}
\setulmarginsandblock{2.5cm}{2.5cm}{*}
\setmarginnotes{2.5mm}{2cm}{1em}
\checkandfixthelayout

\lstdefinelanguage{Groovybash}[]{Groovy}{%
morekeywords={cd,echo,exit,printf,println,print,run,pwd},
morekeywords=[2]{DirectoryNotFound},
morekeywords=[3]{HOME,USER_HOME,CDPATH,PWD}
}

\lstdefinestyle{Groovybash}
{language=Groovybash, style=nonumbers, basicstyle=\small,
emph={},emphstyle=\textbf,
emph={[2]DirectoryNotFound},emphstyle={[2]\textbf},
emph={[3]HOME,USER_HOME},emphstyle={[3]\textbf}}

%% Put the command line parameter in a box
%% \parparameter{Title}
\newcommand{\parparameter}[1]{%
\paragraph{\small\normalfont\noindent\protect\shadowbox{\parbox{\dimexpr\linewidth-2\fboxsep-\fboxrule\relax}{\texttt{#1}}}}
}

\newcommand\parcommand{\textbf}
\newcommand\partype[1]{\texttt{\textsc{#1}}}
\newcommand\codetype{\textsc}

%% put code in double quote
%% \codequoted{text}
\newcommand{\codequoted}[1]{%
``\code{#1}''}

%% put code in double quote
%% \codequoted{text}
\newcommand{\typeitem}[1]{%
$\langle$#1$\rangle$}

\renewcommand{\section}{\@startsection
{section}%                   % the name
{1}%                         % the level
{0mm}%                       % the indent
{-\baselineskip}%            % the before skip
{0.5\baselineskip}%          % the after skip
{\large\textsf}} % the style

\renewcommand{\subsection}{\@startsection
{subsection}%                   % the name
{2}%                         % the level
{0mm}%                       % the indent
{-\baselineskip}%            % the before skip
{0.5\baselineskip}%          % the after skip
{\textsf\normalsize\underline}} % the style

\renewcommand{\subsubsection}{\@startsection
{subsubsection}%                   % the name
{3}%                         % the level
{0mm}%                       % the indent
{-\baselineskip}%            % the before skip
{0.5\baselineskip}%          % the after skip
{\textsf\normalsize\underline}} % the style

\renewcommand{\paragraph}{\@startsection
{paragraph}%                   % the name
{4}%                         % the level
{0mm}%                       % the indent
{0.5\baselineskip}%            % the before skip
{0.25\baselineskip}%          % the after skip
{\normalsize\normalfont}} % the style

\renewcommand{\subparagraph}{\@startsection
{subparagraph}%                   % the name
{5}%                         % the level
{0mm}%                       % the indent
{0.0\baselineskip}%            % the before skip
{0.5\baselineskip}%          % the after skip
{\normalsize\normalfont}} % the style

\DeclareFontFamily{T1}{lmtt}{}
\DeclareFontShape{T1}{lmtt}{m}{n}{<-> ec-lmtl10}{}
\DeclareFontShape{T1}{lmtt}{m}{\itdefault}{<-> ec-lmtlo10}{}
\DeclareFontShape{T1}{lmtt}{\bfdefault}{n}{<-> ec-lmtk10}{}
\DeclareFontShape{T1}{lmtt}{\bfdefault}{\itdefault}{<-> ec-lmtko10}{}

\DeclareFontShape{OT1}{cmtt}{bx}{n}{<5><6><7><8><9><10><10.95><12><14.4><17.28><20.74><24.88>cmttb10}{}
