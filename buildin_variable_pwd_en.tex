\TheVariable{PWD}%
\Variable{PWD}

The absolute path of the current working directory.
The current working directory is changed by the \Command*{cd} command.

\begin{lstlisting}[style=Groovybash, label={lst:example_variable_pwd}]
cd "/some/place"
echo PWD
\end{lstlisting}

