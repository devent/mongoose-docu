%%
\label{par:variable_pwd}
\parparameter{\parcommand{PWD}}

The absolute path of the current working directory.

\subparagraph{Example:}

\begin{lstlisting}[style=Groovybash, label={lst:example_variable_pwd}]
cd "/some/place"
echo PWD
\end{lstlisting}

