\TheVariable[USER_HOME]{USER\_HOME}%
\Variable{USER\_HOME} alias \Variable{HOME}

The home directory of the current user; the default argument for
the \Command*{cd} command.

\begin{lstlisting}[style=Groovybash, label={lst:example_user_home}]
cd HOME
echo HOME
echo USER_HOME
\end{lstlisting}

